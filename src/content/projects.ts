/** @deprecated Import from `@/content` with a locale instead. */
export type { Project } from "./types";
export {
  projects,
  testProjects,
  projectArchive,
  mergedSelflyProjects,
  getProjectBySlug,
} from "./zh/projects";
